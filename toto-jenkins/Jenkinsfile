
pipeline {
    agent {
        label "$NODE"
    }

    tools {
        nodejs "NODE22" //cle : identifiant de outil / version de outil
    }
    stages {
        stage ("Build presentation") {
            steps {
                sh('node -v') // ecrit shell / ""-> warning interpollation / utilsier ''
                sh('npm -v')
            }
        }       
    }
}